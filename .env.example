# Axievale Environment Configuration

# Google AI Studio API Key for AI-powered valuations
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# Google AI Model (default: gemini-1.5-flash)
GOOGLE_AI_MODEL=gemini-2.5-flash

# Server Configuration
PORT=3000
NODE_ENV=development

# Axie Infinity API Configuration
AXIE_GRAPHQL_ENDPOINT=https://graphql-gateway.axieinfinity.com/graphql

# HTTP Request Configuration
MAX_BODY_LENGTH=10485760

# Batch Valuation Configuration
BATCH_VALUATION_LIMIT=10

# Extension configuration
# Optional secret that the extension can include in header `x-extension-secret`
EXTENSION_SECRET=
# Extension cache TTL seconds for extension endpoint
EXTENSION_CACHE_TTL_SEC=60
# Comma-separated list of allowed CORS origins for API (leave empty to allow only server-to-server and chrome-extension origins)
CORS_ALLOWED_ORIGINS=https://marketplace.axieinfinity.com
 # Rate limiting for extension endpoint (per IP)
 EXTENSION_RATE_LIMIT_MAX=60
 EXTENSION_RATE_LIMIT_WINDOW_SEC=60
 # Max entries in extension LRU cache
 EXTENSION_CACHE_MAX_ENTRIES=1000
# Number of trial requests granted to newly registered users
TRIAL_REQUESTS=100
JWT_SECRET=your_jwt_secret_here
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=
# Optional: URLs for success/cancel redirects for Checkout
SUCCESS_URL=
CANCEL_URL=
PROMETHEUS_ENABLED=0
